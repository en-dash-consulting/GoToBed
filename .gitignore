.n-dx.local.json
.rex/n-dx_workflow.md
.rex/execution-log*.jsonl

# Swift / SwiftPM
.build/
*.xcodeproj/xcuserdata/
.swiftpm/
DerivedData/

# Build output (signed app bundle, DMGs) — never commit artifacts
build/

# Local maintainer helper for pushing release secrets (no secrets in it,
# but kept out of the repo by preference)
scripts/set-release-secrets.sh
.run-logs/

# n-dx web server runtime state (auto-managed; not source)
.n-dx-web.port
.n-dx-web.pid

# Ephemeral/local n-dx state (derived cache + timestamped backups)
.rex/.cache/
.rex/.backups/
